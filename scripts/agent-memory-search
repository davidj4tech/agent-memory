#!/usr/bin/env bash
# agent-memory-search - compatibility alias for sacred-search.
#
# Usage:
#   agent-memory-search <query> [user_id] [limit]

set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/sacred-search" "$@"
