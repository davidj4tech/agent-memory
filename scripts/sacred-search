#!/usr/bin/env bash
# sacred-search — legacy alias for agent-memory-search.
#
# Usage:
#   sacred-search <query> [user_id] [limit]

set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/agent-memory-search" "$@"
